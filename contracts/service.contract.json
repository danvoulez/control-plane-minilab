{
  "contract_name": "ServiceContract",
  "version": "0.1.0",
  "purpose": "P0 authority contract for ServiceContract; materializes Control Plane Authority Core language without external effects.",
  "filled_by": "authority_core",
  "validated_by": "minilab-control-plane-authority validators",
  "source_of_truth": "registry/policy/gate/evidence/receipt doctrine; values resolved only from recognized sources or ghosts",
  "required_fields": [
    "service_id",
    "version",
    "role",
    "mode",
    "external_effects_enabled",
    "started_at"
  ],
  "allowed_values": {},
  "forbidden_values": [
    "raw credentials",
    "provider success as closure",
    "caller supplied ok decision",
    "unverified receipt closure"
  ],
  "forbidden_patterns": [
    "raw token values",
    "api key values",
    "password values",
    "private key values",
    "bearer values"
  ],
  "invariants": [
    "natural language never executes",
    "provider success is evidence input not closure",
    "receipts beat stories",
    "unknowns become ghosts not ok",
    "no external effects in v0 validators"
  ],
  "example": {
    "service_id": "minilab-control-plane-authority",
    "version": "0.1.0",
    "role": "control_plane_authority_core",
    "mode": "cli",
    "external_effects_enabled": false,
    "started_at": "2026-05-16T15:00:00Z"
  }
}