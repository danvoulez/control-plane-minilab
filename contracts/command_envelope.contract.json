{
  "contract_name": "CommandEnvelope",
  "version": "0.1.0",
  "purpose": "P0 authority contract for CommandEnvelope; materializes Control Plane Authority Core language without external effects.",
  "filled_by": "caller",
  "validated_by": "minilab-control-plane-authority validators",
  "source_of_truth": "registry/policy/gate/evidence/receipt doctrine; values resolved only from recognized sources or ghosts",
  "required_fields": [
    "command_id",
    "command",
    "actor",
    "input",
    "requested_at",
    "dry_run"
  ],
  "allowed_values": {},
  "forbidden_values": [
    "raw credentials",
    "provider success as closure",
    "caller supplied ok decision",
    "unverified receipt closure"
  ],
  "forbidden_patterns": [
    "raw token values",
    "api key values",
    "password values",
    "private key values",
    "bearer values"
  ],
  "invariants": [
    "natural language never executes",
    "provider success is evidence input not closure",
    "receipts beat stories",
    "unknowns become ghosts not ok",
    "no external effects in v0 validators"
  ],
  "example": {
    "command_id": "cmd_01",
    "command": "gate.decide",
    "actor": {
      "entity_id": "dan",
      "kind": "human"
    },
    "input": {
      "verb": "update_host_runtime",
      "resource_id": "lab256"
    },
    "requested_at": "2026-05-16T15:00:00Z",
    "dry_run": true
  }
}